<%= render 'shared/page_title', title: 'Create New Objective' %> 

<%= render 'shared/errors', obj: @objective %>

<div class='row col-sm-12 '>
	<div class='well col-xs-12 col-md-8 col-md-offset-2 '>
		<%= form_for @objective do |f| %>
			
			<%= f.label :name %> <br/>
			<%= f.text_field :name %> <br/> <br/>
			
			<%= f.submit 'Create Objective', class: 'btn btn-info' %> <br/>

		<% end %>
	</div>
</div>